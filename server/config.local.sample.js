'use strict';

const DEFAULT_CONFIG = require('./config');

module.exports = {
  novelshiveWebsite: DEFAULT_CONFIG.novelshiveWebsite,
  restApiRoot: DEFAULT_CONFIG.restApiRoot,
  host: DEFAULT_CONFIG.host,
  port: DEFAULT_CONFIG.port,
};
